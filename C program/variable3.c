// syntax for const variable :  const variable_name= value;

#include<stdio.h>

int main()
{
	int x = 5, y = 10, z = 15; 		//declare multiple variable at one time	
	printf("%d %d %d",x ,y ,z);			//printing multiple variable
	const num = 10;  //const keyword will declare the variable as "constant", which means unchangeable and read-only:
		//number=15 it will genrate error because number is constant and we can't modify constant variable.	
		
		return 0;
		
}
